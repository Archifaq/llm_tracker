# Copy to .env (never commit the real file) and fill in the keys for the
# providers enabled in config.toml. Loaded via `--env-file .env` or your own
# `set -a; source .env; set +a`; never hardcoded or logged by this tool.
OPENAI_API_KEY=
GEMINI_API_KEY=
PERPLEXITY_API_KEY=
